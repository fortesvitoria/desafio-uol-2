const total = sessionStorage.getItem("total");

document.getElementById("total").innerHTML = total;


function montaResultado (dado){
    var total = document.createElement("p");
    td.textContent = dado;

    return p.toFixed(2);
}
